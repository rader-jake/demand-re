{
  "scripts": {
    "start": "npm --prefix backend start",
    "heroku-postbuild": "npm --prefix backend ci --include=dev && npm --prefix backend run build && npm --prefix backend prune --omit=dev"
  },
  "engines": {
    "node": "20.x"
  }
}
